.venv/
__pycache__/
*.pyc
data/raw/
data/work/
*.egg-info/
.DS_Store

# Bulk corpora pulled for a single experiment do not belong in git. data/asmprov/ is a
# deliberate exception, kept because the inputs to a load-bearing measurement survived there;
# see data/asmprov/README.md.
data/*/*.zip
data/*/*.tar*

# Pivot working data: the source corpus is re-fetched by tools/pivot/fetch_sources.sh and the
# feature caches are regenerated by tools/pivot/run_study.py. Neither belongs in git.
data/pivot/

# Raw GNSS products for the EphemErr study. Re-fetchable by tools/gnss/fetch.sh from BKG and
# ESA's navigation office; ~4.4 MB per day and no reason to carry them in git.
data/gnss/

# 0012 runner heartbeat files change every 30 s and are not provenance
artifacts/pivot/recipe_search_2048_ckpt/*.hb
artifacts/pivot/recipe_search_2048_ckpt/*.tmp
