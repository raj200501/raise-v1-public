# Dependency floors for this repository. Floors, not pins: the preflight checks the floor
# and names the fix when it is not met. Everything here is CPU-only by design.
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
