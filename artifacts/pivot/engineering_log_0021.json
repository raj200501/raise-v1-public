{
  "artifact_identity_at_writing": {
    "commit": "b51395a1a67df7ab3da96b35750d2a157927cb30",
    "duplicate_chunks_artifact_sha256": "229e0070283f8d0ffe838da07dbf3c7f240e9fdd200799e85748d3abdd7b5fe1",
    "prereg_sha256": "194c23b1f77c5dec8365753c51b07b8204b9f35cb2dbb7a45ce226685cbe2ce5",
    "reader_sha256": "19a6f54ce96daddf3de4cf32c905588a1468c2c35655695a011974cc344cd23d"
  },
  "inherited_from_the_0020_review": "0021 carries all sixteen adopted findings of 0020's five-lens pre-freeze review without restating them; they are recorded in artifacts/pivot/engineering_log_0020.json under pre_freeze_review. The two that matter most here are that the sealed corpus blocks are produced by calling the runner's own corpus_block() rather than retyped, and that the mutation gate's control substitutes values into the runner's captured output rather than taking blocks from the reader's literals.",
  "instrument": {
    "fixture": "tests/fixtures/realfit_runner_shape.json, recaptured from an eleven-arm --smoke run against 0021's own smoke block",
    "gate": "tests/mutation_test.py, gate `realfit4096`, repointed at the 0021 reader; 58 cases, 58 detected. New cases cover both directions of every sealed collision count, a missing expectations block, and the fit block's zero staying fatal.",
    "pre_freeze_check": "tools/prereg_checks/prefreeze_0021.py, PASS, with the four collision counts recomputed from data/pivot/full_c4096.npz and data/pivot/realfit_c4096.npz",
    "reader": "tools/readers/realfit4096_rerun_verdict.py, generated from the preregistration and the template; not yet frozen",
    "runner": "tools/pivot/run_realfit.py (shared with 0020, which never ran)"
  },
  "pre_freeze_review": {
    "findings_adopted": [
      {
        "finding": "the reader demands an integer n_iter from four arms whose estimators cannot produce one; four VOID clauses on an honest run",
        "fix": "the requirement is now what the estimator FAMILY produces - n_iter for hgb and logistic, depth and n_leaves for tree, nothing for dummy - and the preregistration's bar says so in those words",
        "lens": "instrument",
        "severity": "high"
      },
      {
        "finding": "at_iteration_cap computes None >= 0 for the same four arms, a TypeError that becomes VOID the moment the first defect is fixed",
        "fix": "the dict is restricted to arms that have an iteration count; fit_info_by_family is banked beside it",
        "lens": "instrument",
        "severity": "high"
      },
      {
        "finding": "the gate's control overwrites fit_info, block_refills and the record environment for all eleven arms, so the control agrees with the reader instead of with the runner",
        "fix": "records are built with _rf_sub against the captured record and those three fields are never touched; a new case checks the reader's per-record requirements against the FIXTURE's own values across all four estimator families, and a second asserts a dummy baseline reporting an n_iter is VOID",
        "lens": "instrument",
        "severity": "high"
      },
      {
        "finding": "the shared runner silently stopped enforcing the FROZEN 0020's condition: a preregistration sealing no expectations got no builder-block check at all, so the published statement that 0020 refused at launch was no longer reproducible from the tree",
        "fix": "absence of the sealed key now means 0020's semantics; verified by re-running the frozen 0020, which refuses again at the same scan and creates nothing",
        "lens": "condition",
        "severity": "high"
      },
      {
        "finding": "the builder cache's 5.76 GB feature array was never hashed by any preregistration - only y and g - and 0021's collision rule was the only thing guarding it, with three integers",
        "fix": "cache_identity seals X_sha256 a28a2d40..., its shape and dtype; the runner hashes and checks it at launch (about 8 s) and banks it, the reader compares it, and a gate case mutates it",
        "lens": "condition",
        "severity": "high"
      },
      {
        "finding": "the bar says 'the collisions are the measured ones' but sealed only how many; a tally of 8 passes on any 8 rows",
        "fix": "the row indices are sealed by sha256 for the repro block, the matched block and the whole pool, and the reader checks all three",
        "lens": "condition",
        "severity": "medium"
      },
      {
        "finding": "the three block counts see only the first 100000 pool rows; 18 of the 26 colliding pool rows sit beyond that and no count would notice a new one there",
        "fix": "the runner scans the whole 800000-row pool and seals pool_rows_identical_to_a_scored_row = 26 with its row-index digest; two gate cases cover it",
        "lens": "condition",
        "severity": "medium"
      },
      {
        "finding": "the sealed block-hash check ran AFTER the collision check, so a divergent permutation would report a confusing collision mismatch rather than naming the block",
        "fix": "reordered",
        "lens": "condition",
        "severity": "medium"
      },
      {
        "finding": "ten of the eleven preregistered readings named 0020's verdict path, which does not exist and never will; tools/render/evidence_page.py was gated on the same two 0020 paths, so an honest 0021 run would have published nothing",
        "fix": "both repointed at the _rerun paths",
        "lens": "instrument",
        "severity": "medium"
      },
      {
        "finding": "bar.complete_one_scoring_per_fit said 'six named records', a leftover from 0020's pre-floor design; there are eleven",
        "fix": "the count is derived from the sealed order rather than typed",
        "lens": "instrument",
        "severity": "medium"
      },
      {
        "finding": "the banked diagnosis artifact said the 32 straddling row pairs were 'spread over 6 of the 9 chunk pairs'; its own table has five nonzero entries. It also divided a PAIR count by a ROW denominator, and said 'the evaluation/pool boundary' where it meant the full non-eval side rather than the capped 800000-row pool",
        "fix": "all three corrected in artifacts/pivot/builder_duplicate_chunks.json, with a corrections_to_this_artifact block recording what was wrong; the distinct-row count is now measured (125) rather than inferred, and the pool-wide count (26) is measured and banked with its row indices",
        "lens": "condition",
        "severity": "medium"
      }
    ],
    "findings_declined": [
      {
        "finding": "bind the artifact's top-level ext_per_family and ext_correct tables to the per-example vectors (instrument lens, L1)",
        "why": "adopted in substance but not by that mechanism: the verdict already republishes its own recomputation of every per-family reading from the vectors, and the reader voids if a banked per-arm reading disagrees with its recomputation. The uncovered case is a fabricated top-level convenience table in the artifact beside a correct verdict. It is worth closing and is filed as an open follow-up rather than done now, because it touches the readings comparison that four frozen readers share."
      }
    ],
    "how": "Two independent read-only reviewers, one lens each - the corrected validity condition and the corpus fact behind it, and whether the frozen reader would read an honest run. Both recomputed from the data rather than reading numbers off artifacts. Neither could write to the repository.",
    "status": "complete; findings applied before the freeze",
    "the_finding_that_justified_the_review": "A THIRD distinct defect that would have voided an honest run, and a third distinct cause. The reader required an integer fit_info.n_iter from all eleven arms. sklearn's DummyClassifier exposes no n_iter_ and no get_depth, and DecisionTreeClassifier exposes get_depth/get_n_leaves but no n_iter_, so run_recipe_search.fit_info() banks {} for the two dummy baselines and {depth, n_leaves} for the two tree baselines - never an iteration count. Four VOID clauses, on the four arms the run's own floor clause depends on. The reviewer proved it by building a faithful real-run artifact from the RUNNER's code and running the frozen reader on it, and proved that fixing two lines was the entire distance between VOID and a verdict. With the n_iter clause alone removed the reader then crashed: at_iteration_cap computed None >= 0 for the same four arms.",
    "the_reproduction_control_quantified": "Asked for a number rather than an argument: the 8 duplicated rows in the 100000-row reproduction block each have exactly one twin among the 260000 evaluation rows, so the largest possible memorisation effect on the banked 0.1965 is 8/260000 = 3.0769e-5, which is 0.62 percent of the 0.005 reproduction tolerance and cannot move the fourth decimal place. The clause's own 38452 rows have zero exposure: no extension row is byte-identical to any of the 1300000 builder rows.",
    "what_the_reviewers_checked_and_found_correct": "Independently recomputed and confirmed: the 9 duplicate chunk pairs and their per-pair row counts summing to 64; the 44496/48016 pair at 26 of 26; all four expected collision counts (0, 8, 2, 0) against the blocks 0021 seals; the row counts 100000/26346/20817/26346 and chunk counts 37108/19413/1029/1029; all seven sealed index hashes and all eight corpus array hashes; the cache X hash; every reader literal - CORPUS, EXT, FIT, all 32 PARTITION keys, both chunk layouts expanding exactly to their arrays, PROTOCOL and RECIPES and all seven recipe hashes; all eleven fingerprints agreeing between the runner's call and the reader's recomputation; STAGE_OF and ROWS_OF against all four dispatch sites; that every top-level, scores-file and record key the reader reads is one the runner writes; that the collision clause voids in BOTH directions on all four blocks and on a deleted expectations block; that the floor is recomputed from the vectors and is the MAXIMUM over the four arms, that clause_per_family uses each family's own floor, and that a model beating chance + 0.05 but not floor + 0.05 reads REAL_FIT_FAILS; and that equality is not brittle, because nothing is recomputed between the disk and the digest - two passes at different thread counts gave identical hashes and counts.",
    "why_the_gate_could_not_see_it": "The same failure as 0020, one nesting level lower. The 0020 review made the gate's control substitute values into the runner's captured blocks for corpus, ext_corpus, fit_corpus and partition - but the FIT RECORDS were still overwritten wholesale, and record() replaced fit_info with {'n_iter': 100} for every arm, handing the reader exactly the value it wanted and the runner never writes. block_refills and the record environment were overwritten the same way. The provenance case was symmetric and recursive over KEY SETS and never compared a value or descended into a record's fit_info, so 58/58 stayed green."
  },
  "preregistration": "0021-realfit-4096-rerun",
  "schema": "raise-v1/engineering_log/1",
  "status": "reviewed, gated at 63/63, pre-freeze-checked PASS; ready to freeze as chain entry 21",
  "what_changed_from_0020": {
    "everything_else": "Unchanged: the eleven arms, the three recipes verbatim from 0014/0018, the four trivial-baseline floor arms, the two builder-matched arms, the seed, the corpora and their hashes, the floor-plus-margin clause, the per-family second clause, the scoring set, the reproduction and null controls, and every disclosure the 0020 review added.",
    "measured_in_advance": "The four counts were measured after 0020's refusal and before 0021 was written, from the same data with the same digest, and are banked in artifacts/pivot/builder_duplicate_chunks.json. The pre-freeze check recomputes them from the data rather than comparing the preregistration against itself.",
    "one_clause": "bar.nothing_scored_was_fitted kept only the counts that bear on the CLAUSE - the four fit-corpus counts, still a blanket zero and still fatal. A new clause, bar.builder_block_collisions_are_the_measured_ones, requires the three BUILDER blocks' counts to EQUAL sealed expectations rather than to be zero.",
    "runner": "tools/pivot/run_realfit.py now measures the plaintext-matched block's collisions too (0020's scan predated that arm), refuses on the fit block separately from the three builder blocks, and banks the sealed expectations on EVERY path including --smoke - because the mutation gate's control is built from a captured smoke artifact, and a key the runner writes only sometimes is a key the control lacks. That is the same class of defect the 0020 pre-freeze review found in the ledger.",
    "the_sealed_counts": {
      "chunk_matched_block": 0,
      "fit_block": 0,
      "matched_block": 2,
      "repro_block": 8
    },
    "why_equality_and_not_a_ceiling": "A ceiling would pass a run where the count grew, which is the case that means new content crossed the boundary. Equality refuses in both directions: larger means something crossed that this preregistration did not measure, smaller means the split, the cache or the blocks are not the sealed ones."
  },
  "why_this_exists": {
    "one_line": "0020 sealed this identical design, froze as chain entry 20, and refused at launch. 0021 changes one validity clause and nothing else.",
    "precedent": "0013 re-read 0012 under a corrected reader; 0019 re-read 0018 under a corrected literal. Neither edited the frozen file and neither is edited here. 0021 is a new preregistration with a new chain entry, a new reader and its own pre-freeze check.",
    "the_cause": "The corpus builder emitted 9 pairs of source chunks whose carved feature rows collide - 64 identical row pairs out of 1300000 rows, 0.00492 percent. The grouped split keeps each CHUNK ID wholly on one side, but it cannot know that two different chunk ids carry the same content, so 32 of those row pairs straddle the evaluation/pool boundary, spread over 6 of the 9 chunk pairs. Only ONE pair (44496 on the evaluation side, 48016 on the pool side, 26 of 26 rows identical) lands inside the blocks 0020 fits on, and it produces every collision the runner refused on: 8 rows of the 100000-row reproduction block and 2 of the 26346-row builder-matched block. NONE of the 9 pairs involves the extension corpus, and 0020's real-content fit corpus has zero rows byte-identical to any scored row, so the clause's own disjointness is intact. What the refusal caught is a property of the BUILDER's corpus, inherited unmeasured by every preregistration that has fitted on this pool and scored on this evaluation set since 0003 - including the 0.1965 reproduction rung 0020 uses as its control.",
    "the_defect_in_0020": "The frozen condition is right for the arm that carries the clause and too strict for the two builder control arms. 'Nothing scored was fitted' is a statement about the FIT corpus; applying it unchanged to the reproduction rung and the builder-matched block asks 0003's eight-year-old pool to have a property the corpus builder never gave it, and makes 0020 unable to run for a reason that cannot touch its own answer. The preregistration says the rival explanation is 'closed by construction'; for the fit corpus it is, and the construction proves it with a measured zero. For the builder blocks the construction refuses instead.",
    "what_0020_did": "Exit 3 at 2026-09-16T08:37:20Z, 7 seconds after launch, before any checkpoint existed, on its own preregistered condition that no fit-block, repro-block or matched-block row may be byte-identical to a scored row. fit was 0; repro was 8 and matched was 2. Filed in artifacts/pivot/engineering_log_0020.json under `refusal`, with the run's stdout banked at artifacts/pivot/realfit_4096_refusal.log. No arm was fitted and nothing was scored."
  }
}
